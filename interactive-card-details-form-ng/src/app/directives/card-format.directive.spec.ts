import { CardFormatDirective } from './card-format.directive';

describe('CardFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CardFormatDirective();
    expect(directive).toBeTruthy();
  });
});
